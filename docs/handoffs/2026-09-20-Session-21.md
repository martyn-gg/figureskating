# Session 21 — 20/09/2026

Cowork, both shells, Playwright already on the Mac. Follows Session 20 on the same day.
**Four commits; `main` was at `02a6ad4` and is now at `aefc1e8`.** Every tree was built and
run through the full chain on its own, by stashing the later work rather than assuming.

One question, which Session 20 opened and could not answer, and which turned out to have a
second half nobody had stated and a fourth fault underneath.

## `skate` names the blade the tracing is built from

Read off the call sites rather than off the name, `skate` was doing three jobs:

- **the rig's spatial root** — `body-frame.js` places every drawn point at `p + (q − sk)`,
  so the reference foot is pinned to the path and the skater hangs off it;
- **the blade the tracing is built from** — `edge` and `dir` attach to it, `secondFoot`
  derives the other foot from it, the ice mark is drawn wherever it goes;
- **a claim that the foot is touching** — `onIceOf` returning `'blade'` for it whatever the
  foot said and whatever its height.

The first two are one job and it is **continuous by construction**: the picture is measured
from the reference, so it exists at every frame or the skater has nowhere to hang. The third
is not continuous at all. **A name that has to be continuous cannot mean a thing that is
not**, which is why the decision could only go one way. Reading `skate` as *the blade that is
touching* would make it change in the middle of a span, and at that instant the hip has
nothing to hang from and `edge` and `dir` have no blade to attach to.

It also finishes a sentence started on 19/09/2026. `onIceOf`'s own comment says the contact
*"is declared on the foot for every other foot in this model; now it is for this one as
well"* — which let the reference blade say **which** contact it had and not that it had
**none**, because the fallback was unconditional. **A default that cannot be overridden is
not a default.**

## The second half, which was not about `skate` at all

`lpP` carries `onIce` from the left key alongside `edge` and `dir`, under a comment calling
all three states rather than quantities. It is right about `edge` and `dir`: they are labels
that stay true while the foot moves. **`onIce` is not that kind of thing.** It is a geometric
claim — this foot is within `ON_ICE` of the ice — and a span is the pose interpolating away
from it. A claim the motion falsifies cannot be carried through the motion.

> At the instant of a key, the key's declaration is the truth. **Between two keys, a contact
> holds only if both ends declare it.**

Which is `arrivalOf`, written the day before to *describe* a state and now *deciding* one.

## Three spans in the whole guide, and the pattern in them is the diagnosis

| move | span | what happens | reference |
|---|---|---|---|
| waltz | 0.30 → 0.32 | L departs, z 2 → 30 | L → air |
| waltz | 0.42 → 0.44 | R arrives, z 26 → 1 | air → R |
| changeFootSpin | 0.36 → 0.50 | L departs z 0 → 16, R arrives z 16 → 0 | L → R |

**In all three the departing foot is the outgoing reference blade.** That is the whole reason
half of a symmetrical piece of work was inert: an arriving foot is not the reference at the
key it starts from, so `onIceOf` found nothing on it and the arrival blend fired; a departing
foot **is**, so `onIceOf` answered `'blade'` for the whole span and `bootDir` returned from
the planted branch before the free one was reached.

And the waltz's authoring says it out loud: the key named *"Blade leaves the ice"* is the one
at 0.32, with the blade thirty centimetres above it. The blade left at 0.30.

## The build: one predicate, one interpolation, no data change

`onIceOf` reads the foot's own declaration first — **including a declared absence** — and
falls back to the reference default only where the foot says nothing. `poseAt` writes
`onIce: null` onto a foot that `arrivalOf` reports arriving or departing, for `u > 0`.
`u > 0` and not `u >= 0` because a blade leaves the ice at a moment, and the last key that
declares the contact is that moment.

**No authored data moved.** Nothing in `moves.js` writes `onIce: null`, so every keyframe
answers exactly as it did and every checker reading `move.keys` is untouched by construction.
Everything reading `poseAt` got the honest answer without being changed. That is the roll's
central claim cashed for the third time.

Deleting the `|| 'blade'` fallback and writing `onIce: 'blade'` into the 85 keys that rely on
it was considered and rejected: at a key the fallback is a derivation, not a guess, and
`twofoot`'s assertion 1 is what holds it honest. **The fallback was never wrong at a key. It
was wrong at a frame, because `skate` carries and the contact it implies expires.**

Third change: `stateOf` in `body-frame.js` asked `!po.skate` and called that airborne, which
reads *"is there a reference"* and was used to mean *"is anything on the ice"* — the same
conflation one layer up. The mark is drawn where the reference blade has a contact now.

**Eleven of 441 hashed frames moved**, all in the two moves with a departure. Five of the
eleven are the waltz's top view at every sampled time after the takeoff, because the mark is
cumulative and ending it six frames earlier changes every later frame of that view.

## `twofoot.mjs`'s assertions 1 and 2, per frame, together

They were per keyframe for the same reason and settling one would have opened the same
argument twice: a foot being put down HAS to cross the 5 cm band, so the clearance bound
could not go per frame until the model could say which feet were changing hands — and a foot
changing hands was exactly what assertion 1 had been calling on the ice.

Per frame they are three claims:

1. a foot claimed on the ice is within `ON_ICE`;
2. a foot claimed free is at least `CLEAR`, **unless** it is changing hands — and above
   `CLEAR` that exception does not bite, so a foot dropped thirty centimetres in one span is
   excused by nothing;
3. **a reference blade that is off the ice is one that is changing hands.**

Mutation counts: air 8, float 91, apart 85, sense 963, bis 4, **carry 39**, dip 3192, stray
13294. `carry` is the one to read — it restores the pre-20/09 reading exactly rather than
inventing a mutation, and thirty-nine is the six waltz frames plus the thirty-three of the
change of foot.

**The monotonic descent the specification asked for is deliberately not asserted.** `lpP`
interpolates `z` linearly between the bounding keys, so a dip is arithmetically impossible,
and *reaches its contact* is assertion 2 holding at the contact key, which it already does.
Both would have been second expressions of facts the model already carries.

## `underice.mjs` gained a run, and that is the fix working

Until this change a departing foot never reached that checker: it was claimed on the ice, so
it was not a free boot and nothing measured it against the ice line. `arriving` became
**`handover`** and takes a list per move — the change of foot's right arriving at 0.0273 cm
and its left leaving at 0.0370 cm, one fault in two directions. The waltz's entry is gone,
the arriving foot having been made neutral the day before.

`--break=deeper` had to move from zeroing the declared depths to **minus one**, because the
new entry's measured depth *is* nought and the mutation had quietly become a no-op on one of
its cases the day it was added. A mutation that no longer moves its case is no longer testing
it.

## The fourth fault, measured and not fixed

**The rig's root jumps at every reference change, and no checker sees it.** Because every
drawn point is `p + (q − sk)`, changing which foot is the reference moves the whole skater by
the old one's offset in a single frame:

| move | seam | the skater moves | as a fraction of the view |
|---|---|---|---|
| waltz | air → R, t = 0.44 | 12.5 cm | **3.0%** |
| waltz | L → air, t = 0.32 | 11.3 cm | **2.8%** |
| changeFootSpin | L → R, t = 0.50 | 8.2 cm | **1.4%** |

against a local rate of 0.3% to 0.6% between neighbouring frames and `continuity.mjs`'s
`SLIDE` bound of 6%. Five to eight times the surrounding motion, sitting at half the bound.
It shows in the **top view only** — the side and rear are drawn from the hip and cannot see
the skater travel across the ice.

This is the reference handover `docs/model.md` calls *a third thing*, and it is untouched by
any of the above: `skate` still carries, so the root behaves byte for byte as it did.
`continuity.mjs` **reports** it now, beside the branch flips, because tightening the bound
onto a fault nobody has fixed turns the chain red without fixing anything, and leaving it
unmeasured is how it stayed invisible through thirteen sessions of that checker.

## What the change of foot looks like now, and the key it is missing

The mark has a **thirty-three-frame gap** through the transfer, at the tightest part of the
coil. Looked at, it reads as a step-over rather than as a missing line, and it is the honest
picture: the authoring has the left blade down at 0.36 and the right at 0.50 and says nothing
about what is touching in between, so drawing a mark there was drawing it from a blade
sixteen centimetres up.

**The repair is an authoring one and was not made.** A transfer key with both blades down
would put them about four centimetres apart, and `twofoot.mjs`'s assertion 3 floors two
blades down at five — a number read off two blades *a leg's width apart*. A spin's change of
foot is the first pose in the guide that is neither that nor a mistake, and ruling on that
floor is its own piece of work.

## Decisions locked

- **`skate` names the blade the tracing is built from.** Reference, not contact. The rig is
  rooted on it and the tracing drawn from it, both continuous by construction.
- **A contact is declared on the foot, for every foot including the reference one**, and a
  declared absence is expressible. The fallback is what the reference means when it says
  nothing.
- **A contact holds across a span only where both keys declare it.** `onIce` is a geometric
  claim, not a label, so it does not carry the way `edge` and `dir` do.
- **The ice mark is drawn where the reference blade has a contact**, not merely where there
  is a reference.
- **A checker's mutation that has stopped moving its case is not testing it** — `--break=deeper`.

## State

**Twenty-four steps in `npm run check`, green end to end on the Mac**, and all four
out-of-band checkers green (`ink`, `framing`, `speed`, `offline`). `npm run ankle` and
`npm run drift` still report rather than fail.

**284 elements, 285 pages, 278 drawing, 7 declared undrawable. Twenty-one rigs in `MOVES`.**
42 transitions, 116 clusters, 48 turns, 32 twizzles, 22 basics, 8 edges, 7 jumps, 5 spins, 3
positions, 1 step. Count with `grep -h '^kind:' src/data/elements/*.md | sort | uniq -c`
rather than trusting this.

| commit | what |
|---|---|
| `2ffc48d` | the question specified, before building it |
| `d570021` | `skate` names the blade the tracing is built from, not the one touching |
| `56859d8` | `twofoot`'s assertions 1 and 2 go per frame, together |
| `aefc1e8` | the reference seam reported, and the specification marked up |

## Open questions

- **The reference handover.** Now the biggest thing open, and it has numbers on it for the
  first time. The rig's root teleports 8 to 13 cm at every reference change. It is also what
  blocks `twoFoot`, `pushOff` and `toePick`, which are held as positions because the rig
  carries one reference blade and drawing what they do means handing it over mid-move.
- **`twofoot`'s assertion 3 floors two blades down at 5 cm**, and the change of foot's
  transfer key would sit at about 4. Decide the floor and the key together.
- **Unchanged**: the two-foot turns' edge letters; the coach numbers, now eight with `CLEAR`;
  the free boot whose direction does not come from the shin alone, still the camel's blocker.
- **31 free-foot keys across six moves still carry `ANKLE_POINT` by default**, which a coach
  disagreed with on the first move he was asked about.

## Prerequisites for the next session

- **Settle which shells the session has before planning anything.** This one had both.
- Playwright is on the Mac, `--no-save`, so it goes after any `npm ci`. Reinstall with
  `npm install --no-save --include=dev playwright`.
- **`twofoot.mjs` and `syllabus.mjs` cannot run in the bridge VM** — they pull `_rig.mjs`,
  which pulls `astro.config.mjs`, which needs rolldown's darwin binding. `underice.mjs`,
  `continuity.mjs`, `rig-names.mjs` and the rest do run there.
- `npm run check` exceeds the desktop MCP's 60s cap. Background it with
  `nohup sh -c '...' >/dev/null 2>&1 & disown` and read the log from the bridge.
- **`git status` through the BRIDGE VM leaves `.git/index.lock`.** Run git on the Mac shell.
- Nothing else. No access, no licence, no external state changed. No customer or personal
  data was touched; the repository holds no PII.

## Artefacts

- `src/lib/rig-math.js` — `onIceOf` reads the foot first including a declared absence;
  `poseAt`'s `foot()` writes `onIce: null` across a handover span
- `src/lib/body-frame.js` — `stateOf` asks for a contact rather than a reference
- `tools/twofoot.mjs` — assertions 1 and 2 per frame, three new breaks
- `tools/underice.mjs` — `arriving` → `handover`, a list per move, departures declared
- `tools/continuity.mjs` — the reference seam reported beside the branch flips
- `docs/model.md` — *What `skate` names*, specified then marked up; `docs/state-of-play.md`
- `shots/session21/` — item 2 done: the elements index and the waltz jump page in both
  schemes, and contact sheets for the waltz and the change of foot. Untracked, as `shots/` is.
- Probes in `_to_delete/` (gitignored): `probe-skate-spans.mjs` lists every span with a
  contact change, `probe-perframe.mjs` is the three per-frame assertions as a count,
  `probe-hip.mjs` measures the root jump in the ice frame, `probe-slide.mjs` is
  `continuity.mjs` patched to print its worst slides, `hashframes.mjs` hashes all 441 frames
  in one process and runs in the bridge VM. Start from those.

## Outstanding

In recommended order.

1. **The reference handover, and the change of foot's transfer key.** One piece of work, not
   two: the key needs `twofoot`'s 5 cm floor ruled on, and the root jump needs the rig to
   have a root that does not depend on which blade is down. It unblocks `twoFoot`, `pushOff`
   and `toePick`, and it closes the mark gap this session opened honestly.
2. **The 31 free-foot keys still on `ANKLE_POINT`**, six moves, held positions first — a
   teapot and an extended edge are positions a coach has a view about.
3. **A free boot whose direction does not come from the shin alone** — the camel change, and
   the last missing rotation after the roll.
4. **Provenance on the basics** — all 22 at `verified: false` on club CDN copies of Learn to
   Skate Basic 1–6. Worth one go at the member portal.
5. **A coach, on the eight numbers** — `ANKLE_POINT`, `ANKLE_MAX`, `MAX_BLADE_PITCH`,
   `CLEAR`, the 60° free-boot limit, `HIP_OUT`, `HIP_IN`, `SKID_MIN_YAW`.
6. **Spins above the positions**: layback and Biellmann want a spine, flying entries the air.
7. **An Instagram landing page** — unchanged, including the view that the existing front page
   should be made to work cold with a deep link per reel.

## Next session, opening prompt

> Read `docs/handoffs/2026-09-20-Session-21.md`, then `docs/model.md`'s section *What
> `skate` names* in full — particularly *What it does not fix, and what measuring it turned
> up*, which is the next piece. Then `figure-skating-workflow.md` and
> `figure-skating-repo-state.md` in memory. Settle which shells the session has before
> planning anything, and check Playwright is still on the Mac with
> `node -e "import('playwright')"`.
>
> Session 21 is committed and pushed; `main` is at `aefc1e8`, every tree in it was built.
>
> **Start with outstanding item 1: the reference handover, and the change of foot's transfer
> key — as one piece of work rather than two.** Every drawn point is `p + (q − sk)`, so
> changing which foot is the reference teleports the whole skater by the old one's offset:
> 12.5 cm and 11.3 on the waltz, 8.2 on the change of foot, which is 1.4% to 3.0% of the top
> view against a local rate of 0.3% to 0.6% and `continuity.mjs`'s bound of 6%.
> `continuity.mjs` reports those three seams now; `_to_delete/probe-hip.mjs` measures them in
> the ice frame and runs in the bridge VM.
>
> The transfer key belongs with it. The change of foot's mark has a thirty-three-frame gap
> because the authoring has the left blade down at 0.36 and the right at 0.50 and nothing in
> between; a key with both down would put them about four centimetres apart, and
> `twofoot.mjs`'s assertion 3 floors two blades down at five, a number read off two blades a
> leg's width apart. Rule on the floor and author the key together — settling one opens the
> same argument twice, which is the shape assertions 1 and 2 had.
>
> Specify it before building it. Four sections have gone that way now and all four were
> marked up afterwards with what they got wrong.
>
> Three standing lessons from this session.
>
> **A default that cannot be overridden is not a default.** `onIceOf` was changed on
> 19/09/2026 to let the reference blade declare WHICH contact it had, and the fallback stayed
> unconditional, so it could not declare that it had none. The half-written sentence sat
> there for a day with its own comment above it.
>
> **Ask what KIND of thing a field is before deciding whether it carries.** `onIce` sat in
> `lpP`'s carried list beside `edge` and `dir` under a comment calling all three states. The
> other two are labels that stay true while the foot moves; `onIce` is a geometric claim, and
> a span is the pose interpolating away from it.
>
> **Measure the thing next to the thing you are fixing.** The root jump was found by
> reconstructing `body-frame.js`'s hip placement per frame while checking whether the
> contact fix would disturb it. It had been there since the rig was built, at half
> `continuity.mjs`'s bound, and nothing was going to trip over it.
