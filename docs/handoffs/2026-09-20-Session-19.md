# Session 19 — 20/09/2026

Cowork, the bridge VM *and* the Mac's own shell, with Playwright already on the Mac. Follows
Session 18, on the same day. **Eight commits; `main` was at `706ae38` and is now at
`deb0754`.** Every intermediate tree was built.

A long one, in four pieces. The morning: one item off the outstanding list, one taken off it
for the opposite reason, a latent sign fault, and a new checker. The afternoon: the last two
model gaps this rig has been carrying — the boot's roll and the flat — which between them
unblocked the lunge, the drag and the slip step, and turned up a tripwire that had been
firing on the live front page unread.

## The two basics were one true claim and one false one

Session 18 left the drag and backward stroking as "the only two basics waiting on nothing but
authoring", with a warning to measure the drag's trailing foot first. The warning was right
to exist and wrong about which wall.

**Backward stroking was waiting on nothing but authoring.** `backStroke` is `pushOff` with the
pelvis facing back down the ice. The yaw of the pushing foot is **the same number in both**,
which looks like it should not be: `bootDir` takes a planted blade's heading from the
direction of travel, 0 forwards and 180 backwards, plus the authored yaw — so going backwards
flips the base, and facing backwards flips which side of the track is the skater's right
because `lateral(180)` is `-n`. The two flips cancel. `turnout.mjs` reads both feet at exactly
35° out. Same property `twoFootTurnBack` found in the yaws of a turn, arriving by the same
route.

What is not mirrored is the body, and that is the half the page is about. Every syllabus
teaches this long after the forward version and not because of the feet, so the shoulder lead
and the forward weight are authored rather than left to the mirror: `sh.t` negative, over the
toes and not the heels. A skater sitting back on a backward push has the blade run away in
front of them, and that is a shape this rig can draw.

**The drag was not**, and the reason took three corrections to reach.

## Three things had to be settled before a number meant anything

**Which way is behind.** `t` is the direction of TRAVEL, so behind the skater is `-t` at
hipYaw 0 and `+t` at hipYaw 180. The first sweep used `+t` at hipYaw 0, which is in FRONT of
the skater. It reported the region as benign — because it is — and would have had me author
keyframes into a wall. `moves.js` opens by warning about exactly this.

**Which sign is up, and this one was a real fault in the repository.** `freefoot.mjs`, which
owns the 60° limit, reads elevation as `asin(bd[2])`, positive toe up. `ankle.mjs` reads
`asin(-bd[2])` and takes the absolute value of it everywhere. **Two expressions of one
quantity with opposite signs, hidden by an `abs()`, since `ankle.mjs` was written.** Fixed,
with the output diffed before and after to prove every printed number unchanged — which is
the point: it was latent, and the next reader who wanted the sign rather than the magnitude
would have had it backwards. It surfaced only because `model.md`'s camel table was read back
against a fresh sweep and came out mirrored.

**What `point` the camel surface was swept at.** It reproduces at **point 25, n −8, hip 96**,
four of five landmarks within a degree and the fifth inside the range it was quoted as. That
is a pointed foot, not the default 10. The elevation surface is a function of the ankle as
well as the leg, so a table read at a different point is a different table. Worth writing on
the table itself, and now is.

## What blocks the drag, corrected mid-session by Martyn

The first version of this section measured a trailing **blade, flat on the ice, turned out** —
which is what the element page said — and concluded `shin.mjs` was the wall. Martyn: **a
drag's free boot is turned IN, the skating knee sinks until the free blade lifts clear, and
what trails is the INSIDE OF THE BOOT.**

So the contact is not a blade, and putting the boot's side down means **rolling** it. That is
the axis `docs/model.md` has said the rig has not got since Session 14 and the one that has
blocked the lunge ever since. **The drag is not a new blocker. It is the lunge's.**

**The model was already saying the blade comes up**, and this was measured before the
correction arrived, so it reads as corroboration rather than a patch:

| hip | furthest back with the marker on the ice | the blade's low end | lift needed |
|---|---|---|---|
| 96 | 32 cm | −8.9 cm | 9.5 cm |
| 92 | 42 cm | −9.9 cm | 10.5 cm |
| 88 | 50 cm | −10.6 cm | 11.0 cm |
| 84 | 56 cm | −11.1 cm | 12.0 cm |

And lifting buys reach: 50 cm back at a hip of 88 with the marker on the surface, 64 at ten
centimetres up, 75 at twenty. Sink, extend, and the blade has to leave the ice. The first pass
read those negative numbers as a pose being blocked; they are the pose saying where the foot
goes. **Read a negative number twice before calling it a wall.**

### The footnote that survives

The pose as the page described it is not drawable either, and it is worth keeping because it
is what the guide would have shipped. A boot allows 28° of shin lean:

| hip | skating blade | trailing, 20 cm | 30 cm | 40 cm |
|---|---|---|---|---|
| 96 | 23° | 26° | 25° | out of reach |
| 94 | 26° | **31°** | **32°** | out of reach |
| 92 | **30°** | **35°** | **37°** | **33°** |
| 88 | **36°** | **41°** | **44°** | **44°** |

Pitching the boot makes it worse monotonically — 44° at zero, 52° at 8, 64° at 20, 80° at 40 —
because tilting the boot tilts its up-axis away from the shin. And `turnout.mjs` reads a
trailing toe as 150° of hip rotation, the case its own header predicted and excluded picks for.

## The checker, and what it found

`tools/underice.mjs` — **a boot that is not on the ice is not drawn below it.** The class of
fault nothing could see, and the deciding one twice now.

It is not covered by `freefoot.mjs`, which limits the boot's ANGLE: the angle is half of where
a boot ends up and the height is the other half, and a boot 30 cm long pointed at the 60°
limit reaches 14 cm below its marker. It measures off the **real render tree** rather than off
the pose, because Session 15's version of this fault was a plan glyph that failed to pivot
about its contact and no correct pose data would have saved it. Where the ice is comes out of
the markup — `data-ice`, added for it.

| mutation | runs |
|---|---|
| clean | 9 over 6,490 glyphs |
| `--break=sink` every free foot 20 cm lower | 20, deepest 14.4 cm |
| `--break=flat` every free foot at z 0 | 30, deepest 17.0 cm |
| `--break=blind` the on-ice exemption removed | 66 over 24,384 glyphs |

A fourth was tried and thrown away, recorded in the header because it is worth knowing:
pointing every free foot to `ANKLE_MAX` took the count **down**, 9 to 6, since plantarflexion
drives the blade wherever the shin already points. A mutation that reduces the count has not
broken anything.

**It dictated a fix rather than confirming one.** Three of the nine are one fault: a boot
built square to the shin gives a free leg sloping down and forward a foot at a right angle to
it — toe up, **heel down** — and it is the heel going through the ice, 5.4 cm of it for
seventy-five consecutive frames on the sit spin, at a boot angle of 58.6° comfortably inside
`freefoot.mjs`'s 60. **An extended free leg is pointed.** `POINTED` at 25, inside `ANKLE_MAX`,
on the keys where the leg is extended and not on the entrance, wind-up and exit. 42 of 399
hashed frames moved, all in the three moves touched.

**It is out of the `check` chain, beside `drift`**, and that is a debt rather than a design.
Two runs survive: the waltz jump's landing, which pointing makes worse, and the change of
foot, whose free foot is at z 0 at the instant it becomes the skating foot — the step-over
this rig does not draw, after `twoFoot` and `toePick`.

## The boot's fourth rotation — the afternoon's first piece

**A boot has three rotational degrees of freedom and this model authored two.** `bootDir`
carries pitch and yaw; the third, about the boot's own long axis, was never authored
anywhere — it was DERIVED in `ankleOf` as the shin's direction with the boot direction taken
out, which pinned the whole boot frame to the plane containing the leg. That is what blocked
the lunge from Session 14 and the drag from this morning.

`roll` on the foot, one rotation inside `ankleOf`, defaulting to zero. **0 of 399 hashed
frames moved.**

**Everything else followed, and that was the specification's strongest claim.** The renderer
holds no boot frame of its own: it recovers the up-axis from the ankle and takes the lateral
as a cross product. So one rotation turns all three axes, the four-glyph chooser picks a plan
view for a boot lying over by itself, and `boot.mjs` still reports the worst disagreement
between a drawn roll and the model's own up-axis as **0.00° across 25,668 glyphs**. No
renderer change at all. A decision made in Session 06 for a different reason paid for this one.

`onIce: 'boot'` is the **fifth contact**. The authored point stays the BLADE'S reference for
all five — a lateral offset was built and taken back out within the hour, because it
contaminates `ank − contact`, the very vector the renderer recovers the up-axis from.
`soleEdgeZ` carries the difference as an assertion, and the pair lives in `blade.mjs`, the
file about which part of the boot reaches the ice: the sole's edge ON the ice, the runner
CLEAR of it. `boot.mjs` excuses a side contact from its roll limit and names `blade.mjs` as
the other side.

**Three things in the specification were wrong** and are marked in `docs/model.md` where they
happened:

- **41.2° is not a threshold**, only the answer for a LEVEL boot. On a lunge's trailing boot
  the sole reaches the ice at about 22. `SIDE_ROLL_LEVEL` is a reference figure.
- **`shin.mjs` cannot be widened to side contacts** — and this reversed a decision Martyn had
  already given, on my framing, which was wrong. `roll` IS the boot's rotation away from the
  plane of the shin, so the shin angle tracks it one for one: 83° against world up at a roll
  of 80. A legal lunge needs **56 to 80° of roll**, which no ankle everts — it is leg
  rotation this rig does not model separately, so no cuff limit applies to `roll` at all.
- **The sole's height is a fixed point, not a linear solve.** Raising the foot re-aims the
  shin, which re-aims the boot; slope about 1.23. The first attempt was 0.8 cm out and
  `blade.mjs`'s new assertion caught it on its first day.

**The drag's rig IS the lunge.** One position, two names, and `docs/model.md` has carried the
note since 30/08/2026 — *coaches call the lunge a drag* — with the instruction that the
element carry the other name in its aliases when it could be drawn. The page is `drag`,
Learn to Skate USA's name; `lunge` is the alias.

## A flat is the absence of an edge — the afternoon's second

The last of the three things *Two blades* said the rig could not hold. Two blades went in on
30/08, the roll an hour earlier, this is the third.

**It was already half built and the halves disagreed.** `PathThumb.astro` has had this in it
since it was written: *"a straight segment has no edge to name and both blades take the
neutral ink"*, so `two-foot-glide` has been drawing an honest flat for weeks and nothing else
knew. `edgeCol` was the other expression — a two-way test over a three-way question,
answering "inside edge" for anything not outside, in four call sites.

**The derived tier was never at risk**, which is what narrowed the job: `EdgeDiagram` runs off
`exitState`, which only ever makes O and I. The gap was the rig.

`edge: null`, not a third letter — BIS name the slip step with no edge letter, and `label` is
foot-then-direction-then-edge, so a letter would have spelled a left forward flat **LFF**.
`lobeSense` returns nought for anything not O or I, so a flat comes out straight from
`buildPath`'s own `-lobeSense/R` with no branch anywhere.

**`lean.mjs` holds a flat rather than excusing it**, and this is the part to keep. Both its
routes are sign tests and a flat has neither sign, so the instinct is to skip it — the hole
its own comments keep warning about. What is true instead is the opposite claim and it is
stronger than either route: **a skater on a flat is not leaning.** The centroid of the flat
blades must sit under the hip, bounded by `BOOT_HALF_W` — not a tolerance but the edge of the
sole, read off the boot glyph for the roll that morning and earning its second use. 320 flat
frames, every one with the hip over the foot.

**No edge dot on a flat**, and that cannot be read off the contact kind because a flat is an
ordinary blade. `boot.mjs` reads the letter from the pose and asserts both ways.

**The slip step**, whose BIS definition is geometry four times over and none of it ours: *"A
step skated in a straight line with the blades of both skates being held flat on the ice. The
weight is over the skating leg that may be well bent or straight while the free foot slides
forward on the ice to full extension."* It is the **first two-foot MOVEMENT this rig has
drawn** — `twoFoot`, `pushOff` and `toePick` are held because the rig carries one reference
blade and drawing what they do means handing it over, and nothing here hands over.

It opens **`kind: 'step'`**, which the enum has carried from the beginning with nothing in it,
ranked 4 between the transitions and the turns. **0 of 420 hashed frames moved.**

## The tripwire that had already fired

`index.astro` kept a hand-written list of the kinds it can name and printed *"N of them in a
category this page has not been taught to name"* for the rest. The spins shipped on
19/09/2026 and **the live front page had been saying "5 of them in a category this page has
not been taught to name" ever since** — shipped, visible, read by nobody.

A tripwire wired into a page instead of the build is a decoration. `element-groups.js` had
already settled this for its own ordering, so the front page throws now.

## Decisions locked

- **A drag's contact is the side of the boot, not a blade.** Martyn. Same missing axis as the
  lunge, and a different one from what the camel change wants.
- **An extended free leg is pointed** — `POINTED` 25, on the keys where the leg is extended.
- **`roll` is authored and asserted**, held by the pair in `blade.mjs` and by nothing else. It
  is NOT an ankle angle and takes no cuff limit.
- **A flat is `edge: null`**, and it is held to the opposite claim from an edge rather than
  excused from both.
- **`kind: 'step'` is open**, ranked between the transitions and the turns.
- **A hand-written list of what to show throws rather than degrading.** Third file to adopt
  it after `element-groups.js` and `drawn.mjs`.
- **`underice.mjs` stays out of the chain** until the waltz landing and the change of foot are
  settled, then goes in with a declared exemption list.

## State

**Twenty-two steps in `npm run check`, green end to end on the Mac, and all four out-of-band
checkers green** (`ink`, `framing`, `speed`, `offline`). Plus `npm run ankle`, `npm run drift`
and `npm run underice`, which report rather than fail.

**279 element pages, 272 drawing, 7 declared undrawable. Twenty-one rigs in `MOVES`.**
Sixteen of twenty-two basics draw. Count with
`grep -h '^kind:' src/data/elements/*.md | sort | uniq -c` rather than trusting this.

**Eight commits, and every intermediate tree was built** — each was checked out and run
through the full chain on its own, so there is nothing here to distrust under a bisect.

| commit | what |
|---|---|
| `22a6abf` | backward stroking's rig |
| `6ed98f3` | the drag measured and found undrawable; the `ankle.mjs` sign |
| `d1bd527` | the drag corrected: the contact is the boot's side |
| `2c9149b` | `underice.mjs`, `data-ice`, `POINTED` on the three spins |
| `0801c16`, `4db05b2` | the handoff, twice |
| `b1108ce` | the boot's fourth rotation, and the drag draws |
| `deb0754` | the flat, the slip step, `kind: 'step'`, the front-page throw |

Two operational notes worth carrying. Running `git status` through the **bridge VM** leaves
`.git/index.lock` behind and blocks every later git write — the Mac shell can `rm` it, the
bridge cannot. And a long job backgrounded from the desktop MCP dies if the call itself times
out at 60s; `nohup sh -c '...' >/dev/null 2>&1 & disown` survives.

## Open questions

- **Unchanged**: the two-foot turns' edge letters; the coach numbers; `rig:` being
  `z.string().optional()` with nothing checking it names a real move.
- **A free boot whose direction does not come from the shin alone** — what still blocks a
  camel in a combination. A different axis from the roll, and now the only one left.

## Prerequisites for the next session

- **Settle which shells the session has before planning anything.** This one had both.
- Playwright is on the Mac, `--no-save`, so it is gone after any `npm ci`. Reinstall with
  `npm install --no-save --include=dev playwright`.
- Nothing else. No access, no licence, no external state changed. No customer or personal data
  was touched; the repository holds no PII.

## Artefacts

- `src/lib/rig-math.js` — `rotateAbout`, `roll` in `ankleOf` and in `lpP`, `BOOT_HALF_W`,
  `BLADE_PROUD`, `SIDE_ROLL_LEVEL`, `bootLateral`, `soleEdgeZ`
- `src/lib/skating.js` — `lobeSense` returns 0 for a flat; `secondFoot` hands back a flat
- `src/lib/body-frame.js` — `data-ice`, three-way `edgeCol`, no dot on a flat
- `src/lib/moves.js` — `backStroke`, `drag` (the lunge), `slipStep`, `POINTED`, `SIDE()`
- `src/pages/index.astro` — throws on an unnamed kind; `src/lib/element-groups.js` — `step`
  ranked
- `tools/underice.mjs` new; `boot.mjs`, `blade.mjs`, `lean.mjs`, `drawn.mjs`, `ankle.mjs`
  amended; `package.json` — `npm run underice`
- `src/data/elements/` — `slip-step.md` new; `drag.md` rewritten; `backward-stroking.md` wired
- `docs/model.md`, `docs/gaps-basics.md`, `docs/state-of-play.md`
- Probes in `_to_delete/` (gitignored): `drag-final.mjs` carries the three measurement
  conventions, `drag-lift.mjs` showed the blade has to come up, `point-fix.mjs` chose 25,
  `lunge-solve.mjs` and `slip-solve.mjs` are the two pose solvers. Start from those if the
  free-boot direction is taken on.

## Outstanding

In recommended order, and the order changed during the session — the afternoon's two model
pieces promoted what was behind them.

1. **The `kind: 'step'` pass.** Newly unblocked and half done: the kind is open and ranked,
   the slip step is in it, and **nine `notCovered` entries** still excuse themselves with some
   version of *"the step wide and the push back"* with nowhere to point. Biggest single
   reduction in unlinked syllabus text left in the guide, and authoring rather than modelling.
   It wants one decision — what a step IS — so bring Martyn the nine entries and a proposal
   read out of BIS's own *Definition of Steps, Turns & Movements* rather than asking him cold.
   Then do all nine in one pass.

2. **Assert that `rig:` names a real move.** Twenty minutes, and it reads differently after
   this session: `rig:` is `z.string().optional()` with nothing checking it, sixteen pages use
   it, all sixteen are fine today — so it is a tripwire rather than a fix, and **two tripwires
   fired unread this session**. Cheap enough to do as the warm-up before item 1.

3. **The two runs `underice.mjs` still reports**, then it goes into the chain. The waltz jump's
   landing (2.0 cm, 25 frames, and pointing the foot makes it WORSE) and the change of foot
   (1.2 cm, 7 frames, free foot at z 0 as it becomes the skating foot — the step-over this rig
   does not draw, after `twoFoot` and `toePick`). Martyn asked to refine these by hand, so it
   needs him in the room; it is item 3 rather than item 1 because a checker parked beside
   `drift` is a known debt, not a bug. When they are settled, wire it in with a declared
   exemption list the way `drawn.mjs` holds its undrawable pages — from both sides, so a named
   case getting worse fails.

4. **Re-shoot `shots/`.** Four PNGs and now five generations stale — the boot fix, the arms,
   `POINTED`, the roll and the flat. The guide's pictures do not match the guide. Mechanical,
   and possible now Playwright is on the Mac.

5. **A free boot whose direction does not come from the shin alone** — the camel change, and
   the last missing rotation after the roll. A model piece; specify it the way the roll and the
   pick were specified.

6. **Provenance on the basics** — all 22 at `verified: false` on club CDN copies of Learn to
   Skate Basic 1–6. Worth one go at the member portal.

7. **A coach, on the seven numbers.**

8. **Spins above the positions**: layback and Biellmann want a spine, flying entries the air.

9. **An Instagram landing page** — unchanged, including the view that the existing front page
   should be made to work cold with a deep link per reel, rather than a second one built.

## Next session, opening prompt

> Read `docs/handoffs/2026-09-20-Session-19.md`, then `docs/model.md`'s last three sections —
> *The drag's contact is the side of the boot*, *The boot's fourth rotation*, and *A flat is
> the absence of an edge* — then `figure-skating-workflow.md` and
> `figure-skating-repo-state.md` in memory. Settle which shells the session has before
> planning anything, and check Playwright is still on the Mac with
> `node -e "import('playwright')"`.
>
> Session 19 is committed and pushed; `main` is at `8f3b1f3`, every tree in it was built.
>
> **Start with the `kind: 'step'` pass — outstanding item 1.** Session 19 opened the kind and
> put one element in it, which leaves it half done: nine `notCovered` entries still excuse
> themselves with some version of "the step wide and the push back" and now have somewhere to
> point. It is the biggest reduction in unlinked syllabus text left, and it is authoring
> rather than modelling.
>
> It rests on one decision — what a step IS — and that is Martyn's. Do not ask him cold:
> enumerate the nine entries first, read BIS's own *Definition of Steps, Turns & Movements*
> (it is in `sources/bis/` and `pdftotext -layout` reads it; it is what gave the slip step its
> four assertions), and bring him a proposal with their wording behind it. Then do all nine in
> one pass rather than one at a time.
>
> Warm up on item 2 if you want something green on the board first: asserting that `rig:`
> names a real move is about twenty minutes and this session found two tripwires that had
> already fired unread.
>
> Two standing lessons, both of which cost real time here:
>
> **Read a negative number twice before calling it a wall.** A blade nine centimetres under
> the ice was not the pose being blocked, it was the pose saying the blade has to come up.
>
> **A tripwire wired into a page is a decoration.** Two fired unread this session — the front
> page's unnamed-kind notice, live since the spins shipped, and `PathThumb` knowing what a
> flat was while nothing else did. If a correctness property matters, make it throw.
